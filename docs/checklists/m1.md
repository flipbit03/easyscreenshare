# M1 smoke checklist — run 2026-08-26, against PRODUCTION

Publisher: headless Chrome, canvas+tone test source (`?testsource=canvas`).
Viewers: 3 concurrent headless Chrome sessions at 1600×900 / 640×400 / 1280×720.

| Check | Result |
|---|---|
| Publisher connects, link minted, LIVE panel | ✅ |
| 3 concurrent viewers receive live video | ✅ all `playing=true` |
| Publisher telemetry | ✅ "3 watching — 3× direct · udp" (aggregated) |
| Audio publish (test tone) + viewer unmute lifecycle | ✅ (verified in earlier run: unmute→playing, re-mute, volume) |
| Adaptive initial layer pick | ✅ all viewers started on 720p layer |
| Manual quality switch | ⚠️ partial — switch commanded; delivered stayed 720p because the **publisher-side encoder** had downscaled the top layer (motion mode = maintain-framerate under synthetic headless load, working as designed). Real-capture verification → field test 3.4 |
| Viewer reconnect (full page reload mid-stream) | ✅ rejoined live |
| Stream end propagation (publisher stop) | ✅ all 3 viewers → "stream ended" instantly |
| Throttled-bandwidth viewer (devtools) | ⏭ deferred to field test 3.4 (needs CDP network emulation or a real bad network) |
| Media path | ✅ direct UDP to 173.199.119.220:7882 (from telemetry chips) |

Environment caveat: headless Chrome throttles rAF, so the canvas source feeds
~15–30 fps; encoder-side numbers in this run are lower bounds, not capability
limits (see roadmap S2 notes for measured cap behavior).
